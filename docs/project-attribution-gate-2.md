# Project-attribution Gate 2 decision record

## Decision and frozen scope

**Decision: APPROVED — reproducible frozen population and classification only.** This does **not** certify installed producer support (Gate 3) or dashboard/probe acceptance (Gate 8). Failures and blocked rows remain preserved for later gates.

The frozen source range is **[2026-08-06T14:53:18.170773000Z, 2026-08-07T17:45:58.546340000Z)**. The immutable raw-universe predicate is `timestamp >= start_inclusive AND timestamp < end_exclusive` over `signoz_traces.distributed_signoz_index_v3` and `signoz_logs.distributed_logs_v2`; traces are deduplicated by `serviceName/spanID`, logs by `resource service.name/id`. [ `docs/project-attribution-gate-2-population-contract.json` `source_time_range`, `raw_universe`; `docs/project-attribution-gate-2-source.json` `query.timestamp_predicate`, `query.identity_deduplication` ] Any predicate change requires a new contract, manifests, checksums, and decision. [ `docs/project-attribution-gate-2-population-contract.json` `refreeze_rule` ]

Inclusion is independent of native key, accepted context, detector output, canonical activity, project tuple, and successful join. Missing/conflicting keys or context remain failed or blocked, never filtered. [ `docs/project-attribution-gate-2-population-contract.json` `inclusion_independence`, `terminal_outcomes`; `docs/project-attribution-gate-2-source.json` `classification` ] No pre-cutoff Gate 3 probe records exist; post-cutoff probes are a separate acceptance population. [ `docs/project-attribution-gate-2-population-contract.json` `probe_exclusion`; `docs/project-attribution-gate-2-delivery.json` `remote_read_only_query.probe_policy` ]

## Frozen identities, backup, and retention

The installed snapshot identifies database `introspection.sqlite3` (SHA-256 `21e489…e05504`), managed runtime `session-context-runtime-v1` (`31c43a…fde6c`), inbox `session-context-inbox` (`aa9a8b…3468f`), and installed scheduler `com.adamjackson.agent-introspection` (observed not running; latest terminal run failed `ScanDeadlineExceeded`). [ `docs/project-attribution-gate-2-installed.json` `local_state.database`, `local_state.managed_runtime`, `local_state.session_context_inbox`, `local_state.scheduler` ] Backup `/Users/adamjackson/.local/share/agent-introspection/backups/introspection-gate-2-20260807T174558546340Z.sqlite3` has SHA-256 `c08c1a…cae73`, integrity check `ok`, and foreign-key check `ok`. [ `docs/project-attribution-gate-2-local.json` `database.backup`; `docs/project-attribution-gate-2-delivery.json` `local_evidence.database_backup` ]

Verified dashboards: Insight UUID `576f5068-d183-5cab-88b7-395f65cf1094`, route `019f4da0-4a13-7c62-9ac9-fc6d850d633b`, revision `v5`, asset `src/agent_introspection/assets/agent-introspection.json`, SHA-256 `69efb9…b994a`; Health UUID `0500ebd3-0d77-4294-b2b5-352ba884daa7`, route `019f7fb0-6f30-77e0-ad12-6d2e44964a7d`, revision `v1`, asset `src/agent_introspection/assets/agent-introspection-health.json`, SHA-256 `cff0c6…5ce7`. [ `docs/project-attribution-gate-2-installed.json` `dashboard` ] Insight SQL ran read-only with `end_timestamp_nano = end_exclusive - 1`. [ `docs/project-attribution-gate-2-delivery.json` `dashboard.execution` ]

Evidence retains only allowlisted identities, timestamps, project tuples, statuses, counts, checksums, and presence checks—never prompts, responses, commands, payload bodies, environment values, or secrets. [ `docs/project-attribution-gate-2-installed.json` `evidence_policy`; `docs/project-attribution-gate-2-delivery.json` `retention_policy` ]

## Population, manifests, and classification

Source manifests under `/Users/adamjackson/.local/share/agent-introspection/project-attribution-investigation/20260807T174558546340Z/` are: `raw-source.jsonl` (12,831,230; `5fb339…8850d`), `included-source.jsonl` (12,830,131; `d50b6a…78b7b`), `excluded-source.jsonl` (1,099; `6a4c5f…ee60`), and `source-sessions.jsonl` (94; `b84afa…0b140`). [ `docs/project-attribution-gate-2-source.json` `manifests`, `counts`; `docs/project-attribution-gate-2-delivery.json` `source_evidence.manifests` ]

Included producer/signal counts: Claude Code trace 1; Codex CLI trace 11,225,945 and log 3,937; Codex app-server trace 1,592,343 and log 1,034; OMP trace 6,871. Excluded counts: unmapped-service trace 6/log 105, Claude log 16, OMP log 972. telemetrygen was present in raw input and excluded as an unmapped service. [ `docs/project-attribution-gate-2-source.json` `coverage.mapped_or_excluded_counts`, `coverage.telemetrygen_exclusion_status`, `coverage.telemetrygen_present_in_frozen_raw_universe` ]

Terminal counts are attributed **3,279**, expected rejection **0**, failed **12,826,851**, blocked **1**. Attribution is exact same-producer/native-session context association: an active interval or a unique same-producer Codex completion context. [ `docs/project-attribution-gate-2-source.json` `counts`, `classification.canonical_join`, `classification.time_semantics`, `classification.terminal_outcome_reasons` ] The single Claude row is blocked pending Gate 3 identity proof. Codex app is blocked because it has neither an independently proven OTEL service/native-identity contract nor a source contract. [ `docs/project-attribution-gate-2-source.json` `coverage.mapped_or_excluded_counts`, `coverage.codex_app_source_contract`; `docs/project-attribution-gate-2-installed.json` `surfaces.claude-code`, `surfaces.codex-app`; `docs/project-attribution-gate-2-population-contract.json` `installed_surface_without_source_contract` ]

Both conservation equations hold: **12,831,230 = 12,830,131 + 1,099** and **12,830,131 = 3,279 + 0 + 12,826,851 + 1**. [ `docs/project-attribution-gate-2-source.json` `counts`, `conservation`; `docs/project-attribution-gate-2-delivery.json` `source_evidence.population` ]

## Local/outbox/remote reconciliation and known failure

Local manifests are `local-context.jsonl` (50; `381f22…4d44`), `local-activities.jsonl` (17; `962c6e…a03`), `local-outbox.jsonl` (93; `1d9281…2bae`), and `local-rejections.jsonl` (470,062; `39b60d…e7dc`). [ `docs/project-attribution-gate-2-local.json` `manifests` ] Remote manifests are `remote-context.jsonl` (30; `380ae3…157`) and `remote-activities.jsonl` (17; `79d47d…fd16`). [ `docs/project-attribution-gate-2-delivery.json` `remote_manifests` ] All 30 accepted contexts and all 17 activities matched: **47 local = 47 matched + 0 local-only** and **47 remote = 47 matched + 0 remote-only**, with no timestamp, status, or project-tuple mismatch. [ `docs/project-attribution-gate-2-delivery.json` `local_to_remote_reconciliation` ] This delivery reconciliation is explicitly distinct from source-population conservation. [ `docs/project-attribution-gate-2-delivery.json` `source_evidence.population.scope`, `local_to_remote_reconciliation.conservation.scope` ]

A prior source query applied `ts_bucket_start`, omitting **1,841,506 deduplicated trace rows**. Raw selection now uses only the frozen authoritative timestamp predicate. This is a confirmed implementation failure, not a population exclusion or producer-support conclusion. [ `docs/project-attribution-gate-2-source.json` `query.ts_bucket_omission_evidence` ]

Gate 1 approved the authority boundary and explicitly required Gate 2 to freeze the cutoff, installed identities, manifests, and conservation while preserving missing key/context/activity as failed or blocked evidence. [ `docs/project-attribution-gate-1.md` “Gate 1 decision and remaining obligations”, “Gate 2 obligations” ]