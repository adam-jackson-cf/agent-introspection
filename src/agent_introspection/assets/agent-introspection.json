{
  "description": "Observed agent behaviours in the selected display range.",
  "layout": [
    {
      "h": 5,
      "i": "project-data-attribution",
      "moved": false,
      "static": false,
      "w": 12,
      "x": 0,
      "y": 0
    },
    {
      "h": 6,
      "i": "actionable-trends",
      "moved": false,
      "static": false,
      "w": 12,
      "x": 0,
      "y": 5
    },
    {
      "h": 6,
      "i": "observed-signals-by-detector",
      "moved": false,
      "static": false,
      "w": 12,
      "x": 0,
      "y": 11
    },
    {
      "h": 5,
      "i": "detector-signal-yield",
      "moved": false,
      "static": false,
      "w": 12,
      "x": 0,
      "y": 17
    }
  ],
  "locked": true,
  "panelMap": {},
  "schemaVersion": 1,
  "tags": [
    "agent-introspection",
    "codex"
  ],
  "title": "Agent Introspection",
  "uploadedGrafana": false,
  "uuid": "576f5068-d183-5cab-88b7-395f65cf1094",
  "variables": {},
  "version": "v5",
  "widgets": [
    {
      "description": "Canonical activity attribution diagnostics in the selected display range.",
      "id": "project-data-attribution",
      "panelTypes": "table",
      "query": {
        "builder": {
          "queryData": [],
          "queryFormulas": []
        },
        "clickhouse_sql": [
          {
            "disabled": false,
            "legend": "Project data attribution",
            "name": "A",
            "query": "SELECT\n  attributes_string['activity.attribution.state'] AS `Attribution state`,\n  if(\n    attributes_string['activity.attribution.state'] = 'resolved',\n    'none',\n    if(\n      empty(attributes_string['activity.attribution.reason_code']),\n      'not recorded',\n      attributes_string['activity.attribution.reason_code']\n    )\n  ) AS `Rejection reason`,\n  attributes_string['activity.producer_surface'] AS `Producer surface`,\n  toFloat64(countIf(\n    attributes_string['activity.attribution.state'] = 'resolved'\n    AND notEmpty(attributes_string['agent.project.id'])\n    AND attributes_string['agent.project.id'] != 'unresolved'\n    AND notEmpty(attributes_string['agent.project.name'])\n    AND attributes_string['agent.project.name'] != 'unresolved'\n  )) AS `Resolved activities`,\n  toFloat64(countIf(attributes_string['activity.attribution.state'] = 'unresolved'))\n    AS `Unresolved activities`,\n  toFloat64(count()) AS `All activities`\nFROM signoz_logs.distributed_logs_v2\nWHERE timestamp BETWEEN $start_timestamp_nano AND $end_timestamp_nano\nAND resource.`service.name`::String = 'agent-introspection'\n  AND attributes_string['event.name'] = 'introspection.activity.version.recorded'\n  AND toUInt64OrZero(toString(attributes_number['activity.payload_schema_version'])) = 2\n  AND notEmpty(attributes_string['activity.id'])\n  AND (\n    attributes_string['activity.id'],\n    attributes_number['activity.version']\n  ) IN (\n    SELECT\n      attributes_string['activity.id'],\n      max(attributes_number['activity.version'])\n    FROM signoz_logs.distributed_logs_v2\n    WHERE timestamp BETWEEN $start_timestamp_nano AND $end_timestamp_nano\nAND resource.`service.name`::String = 'agent-introspection'\n      AND attributes_string['event.name'] = 'introspection.activity.version.recorded'\n      AND toUInt64OrZero(toString(attributes_number['activity.payload_schema_version'])) = 2\n      AND notEmpty(attributes_string['activity.id'])\n    GROUP BY attributes_string['activity.id']\n  )\nGROUP BY `Attribution state`, `Rejection reason`, `Producer surface`\nORDER BY `All activities` DESC, `Attribution state`, `Rejection reason`, `Producer surface`"
          }
        ],
        "queryType": "clickhouse_sql"
      },
      "timePreferance": "GLOBAL_TIME",
      "title": "Project data attribution"
    },
    {
      "description": "Latest canonical activity versions in the selected display range.",
      "id": "actionable-trends",
      "panelTypes": "table",
      "query": {
        "builder": {
          "queryData": [],
          "queryFormulas": []
        },
        "clickhouse_sql": [
          {
            "disabled": false,
            "legend": "Actionable trends",
            "name": "A",
            "query": "SELECT\n  left(attributes_string['activity.id'], 8) AS `Activity`,\n  attributes_string['activity.attribution.state'] AS `Attribution state`,\n  if(\n    attributes_string['activity.attribution.state'] = 'resolved'\n    AND notEmpty(attributes_string['agent.project.id'])\n    AND attributes_string['agent.project.id'] != 'unresolved'\n    AND notEmpty(attributes_string['agent.project.name'])\n    AND attributes_string['agent.project.name'] != 'unresolved',\n    attributes_string['agent.project.name'],\n    'Unresolved'\n  ) AS `Project`,\n  attributes_string['activity.producer_surface'] AS `Producer surface`,\n  attributes_number['activity.version'] AS `Version`,\n  formatDateTime(fromUnixTimestamp64Nano(timestamp), '%d %b %H:%i') AS `Source time`\nFROM signoz_logs.distributed_logs_v2\nWHERE timestamp BETWEEN $start_timestamp_nano AND $end_timestamp_nano\nAND resource.`service.name`::String = 'agent-introspection'\n  AND attributes_string['event.name'] = 'introspection.activity.version.recorded'\n  AND toUInt64OrZero(toString(attributes_number['activity.payload_schema_version'])) = 2\n  AND notEmpty(attributes_string['activity.id'])\n  AND (\n    attributes_string['activity.id'],\n    attributes_number['activity.version']\n  ) IN (\n    SELECT\n      attributes_string['activity.id'],\n      max(attributes_number['activity.version'])\n    FROM signoz_logs.distributed_logs_v2\n    WHERE timestamp BETWEEN $start_timestamp_nano AND $end_timestamp_nano\nAND resource.`service.name`::String = 'agent-introspection'\n      AND attributes_string['event.name'] = 'introspection.activity.version.recorded'\n      AND toUInt64OrZero(toString(attributes_number['activity.payload_schema_version'])) = 2\n      AND notEmpty(attributes_string['activity.id'])\n    GROUP BY attributes_string['activity.id']\n  )\nORDER BY timestamp DESC, `Activity`\nLIMIT 100"
          }
        ],
        "queryType": "clickhouse_sql"
      },
      "timePreferance": "GLOBAL_TIME",
      "title": "Actionable trends"
    },
    {
      "description": "Daily latest canonical activity versions by producer surface in the selected display range.",
      "id": "observed-signals-by-detector",
      "panelTypes": "graph",
      "query": {
        "builder": {
          "queryData": [],
          "queryFormulas": []
        },
        "clickhouse_sql": [
          {
            "disabled": false,
            "legend": "",
            "name": "A",
            "query": "SELECT\n  toStartOfDay(fromUnixTimestamp64Nano(timestamp)) AS ts,\n  attributes_string['activity.producer_surface'] AS producer_surface,\n  toFloat64(count()) AS value\nFROM signoz_logs.distributed_logs_v2\nWHERE timestamp BETWEEN $start_timestamp_nano AND $end_timestamp_nano\nAND resource.`service.name`::String = 'agent-introspection'\n  AND attributes_string['event.name'] = 'introspection.activity.version.recorded'\n  AND toUInt64OrZero(toString(attributes_number['activity.payload_schema_version'])) = 2\n  AND notEmpty(attributes_string['activity.id'])\n  AND (\n    attributes_string['activity.id'],\n    attributes_number['activity.version']\n  ) IN (\n    SELECT\n      attributes_string['activity.id'],\n      max(attributes_number['activity.version'])\n    FROM signoz_logs.distributed_logs_v2\n    WHERE timestamp BETWEEN $start_timestamp_nano AND $end_timestamp_nano\nAND resource.`service.name`::String = 'agent-introspection'\n      AND attributes_string['event.name'] = 'introspection.activity.version.recorded'\n      AND toUInt64OrZero(toString(attributes_number['activity.payload_schema_version'])) = 2\n      AND notEmpty(attributes_string['activity.id'])\n    GROUP BY attributes_string['activity.id']\n  )\nGROUP BY ts, producer_surface\nORDER BY ts, producer_surface"
          }
        ],
        "queryType": "clickhouse_sql"
      },
      "timePreferance": "GLOBAL_TIME",
      "title": "Observed signals by detector"
    },
    {
      "description": "Canonical activity attribution outcomes by producer surface in the selected display range.",
      "id": "detector-signal-yield",
      "panelTypes": "table",
      "query": {
        "builder": {
          "queryData": [],
          "queryFormulas": []
        },
        "clickhouse_sql": [
          {
            "disabled": false,
            "legend": "Detector signal yield",
            "name": "A",
            "query": "SELECT\n  attributes_string['activity.producer_surface'] AS `Producer surface`,\n  toFloat64(countIf(attributes_string['activity.attribution.state'] = 'resolved'))\n    AS `Resolved activities`,\n  toFloat64(countIf(attributes_string['activity.attribution.state'] = 'unresolved'))\n    AS `Unresolved activities`,\n  toFloat64(count()) AS `All activities`,\n  round(\n    100 * toFloat64(countIf(attributes_string['activity.attribution.state'] = 'resolved'))\n    / greatest(toFloat64(count()), 1),\n    2\n  ) AS `Resolved coverage`\nFROM signoz_logs.distributed_logs_v2\nWHERE timestamp BETWEEN $start_timestamp_nano AND $end_timestamp_nano\nAND resource.`service.name`::String = 'agent-introspection'\n  AND attributes_string['event.name'] = 'introspection.activity.version.recorded'\n  AND toUInt64OrZero(toString(attributes_number['activity.payload_schema_version'])) = 2\n  AND notEmpty(attributes_string['activity.id'])\n  AND (\n    attributes_string['activity.id'],\n    attributes_number['activity.version']\n  ) IN (\n    SELECT\n      attributes_string['activity.id'],\n      max(attributes_number['activity.version'])\n    FROM signoz_logs.distributed_logs_v2\n    WHERE timestamp BETWEEN $start_timestamp_nano AND $end_timestamp_nano\nAND resource.`service.name`::String = 'agent-introspection'\n      AND attributes_string['event.name'] = 'introspection.activity.version.recorded'\n      AND toUInt64OrZero(toString(attributes_number['activity.payload_schema_version'])) = 2\n      AND notEmpty(attributes_string['activity.id'])\n    GROUP BY attributes_string['activity.id']\n  )\nGROUP BY `Producer surface`\nORDER BY `Resolved coverage` DESC, `Producer surface`"
          }
        ],
        "queryType": "clickhouse_sql"
      },
      "timePreferance": "GLOBAL_TIME",
      "title": "Detector signal yield"
    }
  ]
}
